procedure miyYMax (E int A[n], S int min, S int max)
	Var
	int i,j
	float x,y
	char c
{
	min <- A[1]
	max <- A[1]
	for(i<-2 to n) do
		if(A[i]<min) then
			min<-A[i]
		else
			if(A[i]>max) then
				max<-A[i]
			endif
		endif
	endfor
}

// Hola luz, el min y max debe ser de salida, sabiendo que en las condiciones se está leyendo la variable?


